{
    "ignore": ["./dist", "./**/*.d.ts"],
    "presets": [
        "@babel/preset-env",
        [
            "@babel/preset-typescript",
            {
                "optimizeConstEnums": true
            }
        ]
    ]
}